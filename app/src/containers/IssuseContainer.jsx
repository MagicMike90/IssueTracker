import React from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';

import ProductsList from '../components/IssueList';

const ProductsContainer = (issue) => (
  <IssueList title="Products">
  </IssueList>
)
